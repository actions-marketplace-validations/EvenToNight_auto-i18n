name: "Translate i18n Files"
description: "Automatically translate JS/TS localization files with Python and deep-translator"
branding:
  icon: 'globe'
  color: 'blue'
inputs:
  source:
    description: "Source language code (e.g., en)"
    required: true
  targets:
    description: "Comma-separated target languages (e.g., it,fr)"
    required: true
  input_file:
    description: "Path to the JS/TS file to translate (e.g., i18n/locale)"
    required: true
  previous_head:
    description: "Git commit hash to compare changes against"
    required: false
    default: "${{ github.event.before }}"
  current_head:
    description: "Current Git commit hash"
    required: false
    default: "${{ github.sha }}"
  evaluate_changes:
    description: "Check if input file has changes respect to the previous head before translating"
    required: false
    default: "true"
  github_token:
    description: "GitHub token for authentication"
    required: false
    default: "${{ github.token }}"
  node_version:
    description: "Node.js version to use (default 20)"
    required: false
    default: "20"
  npm_working_directory:
    description: "Path to the folder where npm commands should run"
    required: false
    default: "."
  pre_commit_npm_command:
    description: "Optional npm command to run pre-commit (e.g. 'lint' or 'lint:fix')"
    required: false
    default: ""
  commit_message:
    description: "Commit message for the translation changes"
    required: false
    default: "chore(i18n): auto-translate i18n files [skip ci]"
  check_only:
    description: "If true, only check that all translation keys are present without translating or committing. Fails if any key is missing."
    required: false
    default: "false"

runs:
  using: "composite"
  steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ inputs.github_token }}

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r ${{ github.action_path }}/requirements.txt
      shell: bash

    - name: Run translation script
      run: python ${{ github.action_path }}/translator.py
      shell: bash
      env:
        INPUT_SOURCE: ${{ inputs.source }}
        INPUT_TARGETS: ${{ inputs.targets }}
        INPUT_INPUT_FILE: ${{ inputs.input_file }}
        INPUT_PREVIOUS_HEAD: ${{ inputs.previous_head }}
        INPUT_CURRENT_HEAD: ${{ inputs.current_head }}
        INPUT_EVALUATE_CHANGES: ${{ inputs.evaluate_changes }}
        INPUT_CHECK_ONLY: ${{ inputs.check_only }}

    - name: Setup Node.js
      if: ${{ inputs.check_only != 'true' && inputs.pre_commit_npm_command != '' }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ inputs.node_version }}

    - name: Run pre-commit command
      if: ${{ inputs.check_only != 'true' && inputs.pre_commit_npm_command != '' }}
      run: |
        echo "Running pre-commit command..."
        npm install
        npm run ${{ inputs.pre_commit_npm_command }}
      shell: bash
      working-directory: ${{ inputs.npm_working_directory }}

    - name: Commit and push translations
      if: ${{ inputs.check_only != 'true' }}
      run: |
        git config --local user.name "github-actions[bot]"
        git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"

        echo "Checking for changes..."
        git status

        git add .

        if git diff-index --quiet HEAD; then
          echo "No changes to commit. Translation files are already up to date."
        else
          echo "Changes detected. Creating commit..."
          git commit -m "${{ inputs.commit_message }}"
          git pull --rebase origin ${{ github.ref_name }}
          git push
          echo "✓ Changes pushed successfully"
        fi
      shell: bash
